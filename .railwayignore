# Don't upload these (reduces deploy size; Railway installs deps and builds)
node_modules
.git
bun.lockb
dist
dist-ssr
*.local

# Large local uploads (PDFs, PPTs, QR codes) – use cloud storage in production
uploads

# Python AI model (if you run it separately or use a different host)
ai_model
__pycache__
*.pyc
*.pkl
*.faiss
.pytest_cache
*.egg-info
.venv
venv
env

# Logs and editor
logs
*.log
.vscode
.idea
.DS_Store

# Env files (set vars in Railway dashboard)
.env
.env.*
!.env.example
